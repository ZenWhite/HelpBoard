import styled from 'styled-components'

export const View = styled.form`
    margin: 32px auto 0;

    display: grid;
    grid-auto-rows: 1fr;
    grid-template-columns: 420px;
    grid-gap: 28px;
    place-items: center;
`